# T-008 Pre-flight — Session/Context Broker

Status: IN_PROGRESS
Selected task: T-008
Base main: 75651581908af7eb3b9b4148106fa142e5707bd9
Dependencies: T-004, T-006, T-007 — published and satisfied.
Blocked successor: T-012 explainable shadow selector.

## Characterization

- ProviderSessionSnapshot already carries authoritative account/model/state/context/workspace affinity.
- Antigravity observation can populate authoritative model/session/context linkage.
- Codex observation intentionally exposes no ProviderSessionSnapshot because thread/context telemetry does not authoritatively identify the selected model.
- Store already exposes account/model/session/latest-capacity reads; no schema migration is required.

## Scope

Implement a provider-neutral, deterministic session/context policy with decisions REUSE / NEW / CHECKPOINT_AND_NEW / UNAVAILABLE. Reuse requires authoritative model affinity, compatible workspace affinity, usable context headroom and account/provider/model health. Add hysteresis without hard-coded provider thresholds; caller supplies policy values. Preserve Codex fail-closed no-inference behavior.

## Protected surfaces

No released migration edits; no scheduler/router/provider-adapter rewrites; no credential/network changes; no new mutable global subsystem; no guessed Codex thread->model binding.

## Verification plan

Targeted decision matrix and exact boundaries; malformed-snapshot fail-closed sentinels; deterministic ordering/property checks; 128-way concurrent decision sentinel plus repository-wide race detector; real SQLite StoreSource characterization; no-inferred-affinity Codex test; full Linux tests, race, vet, existing benchmarks, dedicated broker smoke benchmark and Windows full tests.

## Publication rule

Only publish after living-plan reconciliation and a full green CI on a clean descendant of this pre-flight commit. Re-read remote main immediately before fast-forward. No force.

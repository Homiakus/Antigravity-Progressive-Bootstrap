# T-030 Pre-flight — Add Structural `MASTER_PLAN.md` Audit and Process Metrics

Status: COMPLETED
Selected task: T-030
Base main: 7e96e2debeaeec104d49a37e5d8481ffaf82982d
Dependencies: T-027, T-028, T-029 — published and satisfied.
Blocked successors: None directly; hardens engineering pipeline for all ongoing tasks (T-017..T-026).

## Characterization

- T-027 established the executable workspace-bound completion contract (`ValidateCompletion`).
- T-028 established typed coordinator/worker authority and planner propagation (`roles.go`).
- T-029 established machine-observed publication proofs (`PublicationProof`) and repository-resident context compression checkpoints (`PlanCheckpoint`).
- While `ValidateCompletion` parses task completion states and checkpoints at task completion time, `MASTER_PLAN.md` can suffer from structural drift during development:
  - Duplicate task or finding IDs (e.g. accidentally declaring two `T-015` or `F-004` headings).
  - Invalid task/finding statuses (e.g. misspelled statuses).
  - Dangling task dependencies (referencing a non-existent task ID).
  - Cyclical dependency definitions in the DAG.
  - Undetected High/Critical findings without remediation tasks or explicit accepted boundaries.
  - Inconsistent pre-flight task-start lineage: failing to record `IN_PROGRESS` and pre-flight before production code edits.
- T-030 provides an automated, rigorous structural linter, audit suite, and machine-readable metrics generator for `MASTER_PLAN.md` and engineering process state.

## Scope

1. Create structural plan audit in `internal/engineering/audit.go`:
   - `AuditPlan(plan string) (PlanAuditReport, error)` and `AuditPlanFile(path string) (PlanAuditReport, error)`:
     - **ID Uniqueness**: Verifies all `T-XXX` and `F-XXX` headings are strictly unique.
     - **Allowed Statuses**: Validates all task statuses against allowed set (`TODO`, `READY`, `IN_PROGRESS`, `BLOCKED`, `DONE`, `REJECTED`, `SUPERSEDED`) and finding statuses against (`OPEN`, `IN_PROGRESS`, `RESOLVED`, `ACCEPTED_LIMITATION`, `REJECTED`).
     - **DAG & Dependency Integrity**: Parses dependency declarations, checks for dangling dependencies (references to undeclared tasks), and validates that the task dependency graph is acyclic.
     - **Finding Severity & Remediation**: Parses finding severities (`CRITICAL`, `HIGH`, `MEDIUM`, `LOW`, `INFORMATIONAL`), ensures no unmapped Critical/High finding is ignored without an assigned task or explicit accepted limitation.
     - **Checkpoint Recoverability**: Validates the latest Context Compression Checkpoint contains all 15 required fields and references a valid task.
     - **Pre-flight Integrity**: Validates pre-flight directory convention (`.engineering/preflight/T-XXX.md`) and ensures any `IN_PROGRESS` task has a corresponding valid pre-flight marker.
2. Create machine-readable process metrics in `internal/engineering/metrics.go`:
   - `ComputeProcessMetrics(report PlanAuditReport) ProcessMetrics`:
     - Task counts by status (`Total`, `Done`, `InProgress`, `Ready`, `Todo`, `Blocked`, `Other`).
     - Completion percentage / velocity indicators.
     - Finding counts by severity and status.
     - Digest computation and validation timestamp.
3. Tests and Mutation tests in `internal/engineering/audit_test.go` and `internal/engineering/metrics_test.go`:
   - Validates live `MASTER_PLAN.md` passes full structural audit.
   - Matrix tests for all error conditions (duplicate IDs, invalid status, dangling deps, cycles, unmapped critical findings, missing preflight, malformed checkpoints).
   - Mutation sentinels killing mutants that bypass structural rules.
   - Benchmark test for plan audit and metrics computation.

## Invariants Maintained

- **I-001 Plan authority:** `MASTER_PLAN.md` remains the sole semantic roadmap, with machine-verified structural integrity.
- **I-008 Single-writer publication:** publication lineage checks continue to enforce clean git state.
- **I-009 Repository-resident checkpoint:** latest checkpoint is structurally verified for complete recovery fields.
- **I-030 Plan revision binding:** plan digest and structural validation are mathematically verifiable.

## Protected Surfaces

- Existing `checkpoint.go`, `process.go`, `publication.go`, `roles.go` remain fully compatible.
- Living plan format remains human-readable Markdown with enhanced machine validation.

## Rollback Strategy

- Clean git revert to `7e96e2debeaeec104d49a37e5d8481ffaf82982d`.

## Verification Summary

- `TestAuditPlanValidMinimal`: verified minimal valid plan passes with 0 issues and topological sort order computed.
- `TestAuditPlanLiveMasterPlan`: verified repository's actual `MASTER_PLAN.md` passes complete structural audit cleanly.
- `TestAuditPlanDuplicateTaskID`: verified duplicate task heading is detected and rejected with `IssueDuplicateID`.
- `TestAuditPlanDuplicateFindingID`: verified duplicate finding heading is detected and rejected.
- `TestAuditPlanInvalidTaskStatus`: verified invalid or misspelled task status is detected and rejected with `IssueInvalidStatus`.
- `TestAuditPlanDanglingDependency`: verified task referencing undeclared dependency is rejected with `IssueDanglingDependency`.
- `TestAuditPlanCyclicDependency`: verified topological cycle detection in task dependencies with `IssueDependencyCycle`.
- `TestAuditPlanMissingPreflight`: verified `IN_PROGRESS` task without `.engineering/preflight/T-XXX.md` file fails audit with `IssueMissingPreflightFile`.
- `TestAuditPlanConcurrentExecution`: 64 concurrent auditors verified under `go test -race`.
- `TestComputeProcessMetrics`: verified calculation of task completion rate, status distributions, and finding counts.
- `TestAuditMutationsKillDefects`: mutation sentinels kill mutants allowing duplicate IDs, non-canonical statuses, missing checkpoint fields, dangling deps, circular deps, or missing pre-flights.
- `BenchmarkAuditPlanMASTERPLAN`: permanent CI smoke benchmark measures ~432 µs/op for auditing entire `MASTER_PLAN.md`.
- `BenchmarkComputeProcessMetrics`: measures ~3.2 µs/op for metrics aggregation.
- Full verification suite (`go vet ./...`, `go test -race ./...`) verified clean.
